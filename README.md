# nail_disease_detection
Nail Disease Detection is a deep learning–powered web application that identifies nail-related diseases from uploaded images. The system uses a trained Convolutional Neural Network (CNN) model integrated with a Flask web interface to provide disease predictions along with symptoms, causes, and care recommendations.
